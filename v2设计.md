# 克苏鲁守秘人轻量版设计文档：Agent / Tools / 游戏助手改造版

本文是在当前 `coc-lite` 设计基础上的修改版，重点改造三部分：

1. 重构 `KeeperAgent`，引入 **Plan-and-Solve、ReAct、Reflection** 三种互补 Agent 范式。
2. 将可复用、且需要 LLM 在多个候选动作中自主选择的能力抽象为 **Tools / Skills**，但保留状态读写、校验、持久化等确定性流程为代码写死。
3. 前端增加一个可收起的 **游戏助手对话栏**，用于规则问答、术语解释、非剧透提示，并在检索上使用 **MQE、HyDE、统一扩展检索框架**。

---

# 一、项目定位调整

项目仍然定位为：

**单玩家《克苏鲁的呼唤》AI 守秘人网页版原型**

但 Agent 形态从原来的“线性 LangGraph 节点流水线”升级为：

**计划驱动 + ReAct 执行 + 反思修正的可控守秘人 Agent**

新的核心目标：

1. 玩家仍通过自然语言行动。
2. AI 守秘人不再直接从输入跳到裁定，而是先生成完整行动计划。
3. 每个回合先由 Plan-and-Solve 规划，再由 ReAct 在受限工具/技能集合中执行。
4. 每次生成结果必须经过 Reflection 自检与确定性 guardrails 校验。
5. LLM 可以选择合适工具和技能，但不能直接写数据库、修改状态或绕过校验。
6. 前端提供独立的“游戏助手”对话栏，帮助玩家查询规则，不推进剧情、不修改游戏状态。

---

# 二、KeeperAgent 结构改造

## 1. 三种 Agent 范式的分工

| 范式 | 在项目中的职责 |
|---|---|
| Plan-and-Solve | 每回合开始时，先生成完整行动计划，包括意图、目标、所需上下文、可能检定、允许使用的 Tools / Skills、预期状态变化和失败处理 |
| ReAct | 严格在计划允许范围内执行：边观察、边选择工具或技能、边根据结果调整执行细节 |
| Reflection | 在叙事和状态变更提交前，对规则一致性、剧情一致性、防剧透、状态合法性和玩家体验进行自我批判与修正 |

三者关系：

- **Plan-and-Solve** 负责“先想清楚再做”。
- **ReAct** 负责“执行时根据观察结果动态处理”。
- **Reflection** 负责“做完后检查有没有错”。

但 ReAct 不能无限自由行动。

新的约束原则：

> ReAct 只能在 Plan-and-Solve 生成的计划白名单内选择 Tools / Skills。
> 如果执行中发现需要计划外动作，不能直接调用，而是返回 `plan_gap`，由代码路由到重新规划或澄清节点。

---

# 三、KeeperAgent 新执行流程

原流程：

`load_state → parse_intent → retrieve_context → adjudicate → roll_tools → resolve_action → generate_response → generate_state_delta → validate → secret_leak_check → options → commit`

修改后流程：

1. `load_state`
2. `build_visible_context`
3. `plan_turn`
4. `validate_plan`
5. `clarify_action`，仅当计划判断输入不足或歧义过高时进入
6. `execute_plan_react`
7. `synthesize_resolution`
8. `generate_response`
9. `generate_state_delta`
10. `deterministic_guardrails`
11. `reflection_review`
12. `repair_or_replan`
13. `final_guardrails`
14. `generate_next_options`
15. `commit_state`

## 1. 节点职责调整

| 节点 | 类型 | 职责 |
|---|---|---|
| `load_state` | 代码写死 | 从 PostgreSQL 读取会话、角色、线索、物品、flag、回合日志 |
| `build_visible_context` | 代码写死 | 构建当前玩家可见状态，区分玩家可见信息和守秘人秘密 |
| `plan_turn` | LLM，Plan-and-Solve | 生成完整回合计划，包括行动目标、所需资料、可能检定、允许工具和技能 |
| `validate_plan` | 代码写死 | 检查计划格式、工具白名单、风险等级和是否越权 |
| `clarify_action` | LLM + 固定模板 | 当行动模糊、不完整或无法安全推进时，生成澄清问题 |
| `execute_plan_react` | LLM + Tools / Skills，ReAct | 在计划约束下选择工具或技能，读取观察结果并继续执行 |
| `synthesize_resolution` | 代码 + LLM 辅助 | 汇总检索、技能检定、骰点和剧情上下文，形成结构化裁定 |
| `generate_response` | LLM 固定节点 | 生成守秘人叙事，不直接写状态 |
| `generate_state_delta` | LLM + 结构化收敛 | 从叙事和裁定中提取候选状态变化 |
| `deterministic_guardrails` | 代码写死 | 校验地点、场景、时间、危险值、线索、物品和 flag |
| `reflection_review` | LLM，Reflection | 自我批判：规则、剧情、防剧透、状态合法性、叙事质量 |
| `repair_or_replan` | 代码路由 + LLM 修复 | 根据 Reflection 结果进行文本修复、状态修复、重新规划或澄清 |
| `final_guardrails` | 代码写死 | 最终防剧透、状态校验和输出清洗 |
| `generate_next_options` | LLM + 模板 | 生成玩家可见的下一步行动建议 |
| `commit_state` | 代码写死 | 落库、写日志、写长期记忆、同步线索和物品 |

---

# 四、Plan-and-Solve 设计

## 1. 回合计划内容

`plan_turn` 不直接生成叙事，而是生成一个结构化回合计划。

计划应包含：

| 字段 | 说明 |
|---|---|
| `intent` | 玩家行动意图 |
| `goal` | 本回合要解决的核心目标 |
| `assumptions` | Agent 对玩家输入的必要假设 |
| `needs_clarification` | 是否需要澄清 |
| `clarification_question` | 需要澄清时的问题 |
| `action_type` | 行动类型，例如调查、移动、交谈、使用物品、等待、战斗倾向等 |
| `required_context` | 需要检索的上下文类型 |
| `allowed_tools` | 本回合允许调用的工具白名单 |
| `allowed_skills` | 本回合允许调用的技能白名单 |
| `possible_checks` | 可能需要的技能、属性、幸运或理智检定 |
| `risk_level` | 行动风险等级 |
| `expected_state_delta` | 预期可能发生的状态变化范围 |
| `success_criteria` | 判断本回合是否完成的条件 |
| `fallback` | 失败或信息不足时如何处理 |

## 2. 计划执行约束

Plan-and-Solve 的计划不是参考意见，而是执行约束。

执行规则：

1. ReAct 只能调用计划中列出的 Tools / Skills。
2. 如果需要调用计划外工具，必须停止当前执行并返回 `plan_gap`。
3. `plan_gap` 由代码决定：
   - 重新规划一次；
   - 或要求玩家澄清；
   - 或退回安全叙事，不推进状态。
4. 同一回合最多允许一次重新规划。
5. 如果重新规划后仍无法安全执行，则不推进剧情，只返回澄清问题。

---

# 五、ReAct 执行设计

## 1. ReAct 在项目中的作用

ReAct 不负责全局自由探索，而是负责在单个计划内执行。

典型过程：

1. 根据计划中的当前步骤，观察已有状态。
2. 从允许的 Tools / Skills 中选择一个动作。
3. 调用工具或技能。
4. 读取观察结果。
5. 判断是否继续、是否需要额外检索、是否需要检定。
6. 直到计划完成、达到调用上限、或发现计划缺口。

## 2. ReAct 输出

不向玩家暴露模型的完整思考链。

系统只保留：

| 字段 | 说明 |
|---|---|
| `react_trace` | 简化后的动作轨迹 |
| `tool_calls` | 调用过的工具及输入摘要 |
| `observations` | 工具返回的结构化观察 |
| `decision_summary` | 简短执行理由 |
| `plan_gap` | 是否发现计划外需求 |
| `execution_status` | 成功、失败、部分成功、需要澄清 |

注意：

> 不在前端展示原始 Chain-of-Thought。
> 调试面板最多展示经过压缩的执行摘要和工具调用列表。

---

# 六、Reflection 设计

## 1. Reflection 触发时机

Reflection 在以下阶段执行：

1. 守秘人叙事初稿生成后；
2. `state_delta` 初稿生成后；
3. 确定性 guardrails 给出初步校验报告后；
4. 最终提交数据库之前。

## 2. Reflection 检查维度

| 维度 | 检查内容 |
|---|---|
| 剧情一致性 | 是否符合当前场景、地点、时间、已触发剧情 |
| 规则一致性 | 是否错误使用技能、难度、骰点、理智损失 |
| 防剧透 | 是否暴露未发现线索、隐藏 NPC 动机、剧本真相 |
| 状态合法性 | 是否非法移动、非法获得物品、非法发现线索 |
| 玩家公平性 | 是否无理由惩罚玩家，是否剥夺玩家选择 |
| 叙事质量 | 是否清晰、有氛围、不过度替玩家行动 |
| 计划遵循度 | 是否偏离了本回合 Plan-and-Solve 计划 |

## 3. Reflection 结果

Reflection 输出结构化结论：

| 结果 | 后续处理 |
|---|---|
| `pass` | 进入最终 guardrails |
| `repair_text` | 重写或清洗叙事 |
| `repair_state_delta` | 修正状态变化 |
| `rerun_tool` | 允许重新执行某个已在计划白名单内的工具 |
| `replan_once` | 触发一次重新规划 |
| `ask_clarification` | 不推进剧情，要求玩家澄清 |
| `fail_safe` | 输出安全兜底叙事，不写入高风险状态变化 |

确定性 guardrails 的优先级高于 Reflection。

也就是说：

> Reflection 可以提出修复建议，但最终是否允许写入状态，由代码校验决定。

---

# 七、Tools / Skills 设计原则

## 1. 核心原则

本项目不把所有操作都做成 Tools / Skills。

只有满足以下条件的能力才设计为 Tool 或 Skill：

1. LLM 需要在多个候选动作中自主选择；
2. 能力可复用；
3. 输入输出可以结构化；
4. 调用结果可被审计；
5. 不直接绕过状态校验和数据库提交。

以下操作不做成 Tool / Skill，继续代码写死：

| 操作 | 原因 |
|---|---|
| 加载会话和角色 | 固定流程，不需要 LLM 选择 |
| 写入数据库 | LLM 不应直接决定持久化 |
| 应用 `state_delta` | 必须经过代码校验 |
| 防剧透清洗 | 安全边界，不能交给 LLM 自由决定 |
| API 路由 | 固定 Web 层逻辑 |
| NDJSON 流式输出协议 | 固定传输逻辑 |
| 导入剧本和规则书 | 管理功能，不是回合内自主选择 |
| Chroma 底层 embedding / upsert | 基础设施，不暴露给 Agent |
| PostgreSQL 事务提交 | 必须由代码控制 |

---

# 八、Tools 设计

Tool 是原子能力，通常是窄输入、窄输出、可审计、无持久化副作用的函数。

## 1. KeeperAgent 可用 Tools

| Tool | 用途 | 是否有副作用 |
|---|---|---|
| `ContextSearchTool` | 检索剧本、规则、实体、线索索引、会话记忆 | 无 |
| `RuleCheckTool` | 执行技能检定、属性检定、幸运检定、理智检定 | 仅产生本回合结果，不直接落库 |
| `InventoryLookupTool` | 查询当前物品和可用状态 | 无 |
| `SceneAffordanceTool` | 查询当前地点可交互对象、出口、NPC、风险元素 | 无 |
| `ClueEligibilityTool` | 判断某线索是否可能被当前行动发现 | 无 |
| `MemoryRecallTool` | 检索当前会话长期记忆 | 无 |

## 2. Tools 使用约束

1. Tool 只能在 `execute_plan_react` 中被调用。
2. Tool 必须在 `allowed_tools` 白名单中。
3. Tool 返回结构化 observation。
4. Tool 不直接修改 `GameSession`、`Clue`、`InventoryItem`、`StoryFlag`。
5. 骰点类 Tool 的结果必须写入 `react_trace`，并由 `commit_state` 统一记录。

---

# 九、Skills 设计

Skill 是比 Tool 更高一层的可复用行动能力，可以内部调用多个 Tool。

Skill 适合处理“玩家行动类型”，例如调查、移动、交谈、使用物品等。

## 1. KeeperAgent Skills

| Skill | 适用场景 | 可能调用的 Tools |
|---|---|---|
| `InvestigateSkill` | 检查房间、物品、尸体、痕迹、文件、机关 | `ContextSearchTool`、`ClueEligibilityTool`、`RuleCheckTool` |
| `MoveSkill` | 前往新地点、进入房间、离开区域 | `SceneAffordanceTool`、`ContextSearchTool` |
| `SocialInteractionSkill` | 与 NPC 交谈、威胁、说服、心理观察 | `ContextSearchTool`、`RuleCheckTool`、`MemoryRecallTool` |
| `UseItemSkill` | 使用、组合、展示、丢弃物品 | `InventoryLookupTool`、`SceneAffordanceTool`、`RuleCheckTool` |
| `DangerAndSanitySkill` | 处理恐怖场景、怪异发现、精神冲击 | `ContextSearchTool`、`RuleCheckTool` |
| `CombatLiteSkill` | 轻量冲突、逃跑、躲避、临时对抗 | `RuleCheckTool`、`SceneAffordanceTool` |
| `WaitOrObserveSkill` | 等待、倾听、观察环境变化 | `ContextSearchTool`、`MemoryRecallTool` |

## 2. Skill 与代码职责边界

Skill 可以：

- 判断当前行动是否需要检索；
- 判断是否需要检定；
- 调用允许的 Tool；
- 返回候选裁定结果；
- 生成结构化观察。

Skill 不可以：

- 直接推进剧情阶段；
- 直接写入线索；
- 直接增加或删除物品；
- 直接改变地点；
- 直接提交数据库；
- 绕过防剧透检查。

---

# 十、KeeperState 更新

新增字段：

| 字段 | 说明 |
|---|---|
| `turn_plan` | Plan-and-Solve 生成的完整行动计划 |
| `plan_validation` | 计划校验结果 |
| `react_trace` | ReAct 执行动作轨迹 |
| `tool_observations` | 工具返回的结构化观察 |
| `skill_results` | 技能执行结果 |
| `plan_gap` | 是否发现计划外需求 |
| `reflection_report` | Reflection 自检报告 |
| `repair_attempts` | 本回合修复次数 |
| `final_guardrail_report` | 最终校验报告 |
| `visible_context` | 玩家可见上下文 |
| `keeper_only_context` | 仅守秘人可见上下文，不可直接输出给玩家 |

原字段保留：

- `intent`
- `scenario_context`
- `rule_context`
- `entity_context`
- `clue_context`
- `memory_context`
- `adjudication`
- `dice_results`
- `skill_checks`
- `sanity_checks`
- `resolution`
- `generated_payload`
- `narration`
- `options`
- `state_delta`
- `validation_report`
- `leak_report`
- `audit`
- `summary`
- `discovered_clues`

---

# 十一、前端新增：可收起游戏助手对话栏

## 1. 功能定位

新增“游戏助手”不是守秘人本体，而是一个 **场外规则助手 / 游戏帮助助手**。

它负责：

- 回答 COC 规则问题；
- 解释技能、属性、检定、理智、困难成功等概念；
- 说明网页操作；
- 回顾玩家已经发现的线索；
- 给出非剧透的行动建议；
- 引用规则书或已发现信息来源。

它不负责：

- 推进剧情；
- 代替守秘人裁定；
- 修改角色状态；
- 掷骰；
- 透露未发现线索；
- 解释剧本真相。

## 2. 前端交互设计

新增组件：

| 组件 | 说明 |
|---|---|
| `GameAssistantPanel` | 可收起的游戏助手侧栏 |
| `AssistantChat` | 对话消息列表 |
| `AssistantInput` | 输入框 |
| `AssistantCitationList` | 展示规则来源和引用片段 |
| `AssistantModeToggle` | 可选：规则问答 / 当前局势帮助 |
| `AssistantSettings` | 可选：是否启用 HyDE、显示引用、显示检索来源 |

界面行为：

1. 默认以悬浮按钮或窄侧栏形式收起。
2. 点击后展开为右侧抽屉或底部面板。
3. 桌面端建议宽度 360px 到 420px。
4. 移动端使用全屏抽屉。
5. 收起状态写入 `localStorage`。
6. 助手聊天记录与主游戏叙事分离。
7. 助手回答支持流式输出。
8. 回答末尾展示引用来源，例如规则书章节、调查员手册片段、已发现线索名称。

## 3. 与现有右侧状态栏的关系

当前右侧已有：

- 物品栏；
- 材料与地图；
- 线索簿；
- 剧情状态；
- 场景记录；
- 调试信息。

游戏助手不替代这些面板，而是独立为：

- 右下角浮动入口；
- 或右侧状态栏中的一个可展开标签页；
- 或独立抽屉层。

推荐方案：

> 桌面端使用右下角浮动按钮 + 右侧抽屉。
> 移动端使用底部按钮 + 全屏抽屉。

---

# 十二、游戏助手后端设计

新增轻量 `GameAssistantAgent`，与 `KeeperAgent` 分离。

## 1. 设计原则

| 原则 | 说明 |
|---|---|
| 不推进剧情 | 助手不会调用 `run_turn` |
| 不写游戏状态 | 不修改 `GameSession`、`Clue`、`InventoryItem` |
| 默认查规则 | 优先检索规则书和调查员手册 |
| 可读当前可见状态 | 只读取玩家已知的线索和摘要 |
| 强防剧透 | 不访问或不输出守秘人秘密 |
| 必须引用来源 | 规则回答尽量附带出处 |

## 2. 助手处理流程

游戏助手请求流程：

1. 接收玩家问题。
2. 判断问题类型：
   - 规则问题；
   - UI 操作问题；
   - 当前局势帮助；
   - 术语解释；
   - 可能剧透问题。
3. 构建允许检索的命名空间。
4. 使用扩展检索框架：
   - 原始查询；
   - MQE 多查询扩展；
   - HyDE 假设文档；
   - 合并、去重、排序。
5. 生成回答。
6. 执行防剧透检查。
7. 返回流式回答和引用来源。

---

# 十三、游戏助手 RAG：MQE、HyDE、统一扩展检索

## 1. 多查询扩展 MQE

MQE 用于解决玩家提问表达不稳定的问题。

例如玩家问：

“困难成功怎么算？”

系统可以扩展为：

- “COC 困难成功 判定”
- “克苏鲁的呼唤 技能检定 成功等级”
- “普通成功 困难成功 极难成功 区别”

设计要求：

| 项 | 设计 |
|---|---|
| 默认启用 | 是 |
| 默认扩展数量 | 2 到 3 条 |
| 生成方式 | LLM 生成语义等价或互补的短查询 |
| 语言 | 中文 |
| 用途 | 提升召回率 |
| 失败回退 | 使用原始查询 |

MQE 适合：

- 模糊规则问题；
- 玩家用词和规则书术语不同；
- 同一概念存在多种叫法；
- 玩家只记得大概机制。

## 2. 假设文档嵌入 HyDE

HyDE 用于解决“问题”和“答案文本”在语义空间中不完全匹配的问题。

流程：

1. 玩家提出问题；
2. LLM 先生成一段“可能的答案性段落”；
3. 系统不直接把这段当答案；
4. 而是将它作为检索查询文本；
5. 用它去匹配真实规则文档。

设计要求：

| 项 | 设计 |
|---|---|
| 默认策略 | 复杂规则问题自动启用 |
| 可配置 | 前端调试或后端参数可强制开启 |
| 用途 | 提升专业术语、规则段落的匹配率 |
| 是否展示给玩家 | 不展示 HyDE 原文 |
| 失败回退 | 忽略 HyDE，仅使用原查询和 MQE |

HyDE 适合：

- “理智检定失败会怎样？”
- “临时疯狂和不定性疯狂区别？”
- “技能成长什么时候检定？”
- “对抗检定如何比较成功等级？”

## 3. 统一扩展检索框架

游戏助手统一使用“扩展-检索-合并-重排-回答”流程。

### 第一步：扩展

扩展查询集合包括：

1. 原始问题；
2. MQE 生成的多个查询；
3. HyDE 生成的假设答案段落。

### 第二步：检索

每个扩展查询都会进入向量检索。

检索范围根据问题类型控制：

| 问题类型 | 检索范围 |
|---|---|
| 规则问题 | `rule_chunks`、调查员手册、玩家可见规则 |
| UI 问题 | 前端帮助文档、项目内置说明 |
| 当前局势帮助 | 已发现线索、当前会话摘要、玩家可见记忆 |
| 剧本真相问题 | 不检索守秘人秘密，返回防剧透提示 |

### 第三步：合并去重

多个查询可能命中同一片段。

合并时按以下 ID 去重：

- `memory_id`
- `chunk_id`
- `source_path + chunk_index`

同一文档被多次命中时，保留最高分或融合分数。

### 第四步：排序

排序综合考虑：

1. 向量相似度；
2. 是否来自规则书；
3. 是否来自玩家手册；
4. 是否是当前会话可见信息；
5. 是否与原始问题直接匹配；
6. 是否重复命中。

### 第五步：回答

回答生成要求：

1. 优先基于检索内容回答。
2. 不确定时明确说明“不确定”。
3. 不编造规则。
4. 给出简洁示例。
5. 附带引用来源。
6. 不透露未发现剧本秘密。

---

# 十四、RAG 元数据调整

为支持游戏助手和防剧透，导入 Chroma 时需要强化元数据。

新增或规范以下字段：

| 字段 | 说明 |
|---|---|
| `rag_namespace` | 文档命名空间，例如 `rules`、`scenario`、`session_memory` |
| `source_type` | `rulebook`、`investigator_handbook`、`scenario_public`、`scenario_secret`、`clue`、`memory` |
| `visibility` | `public`、`player_visible`、`keeper_only` |
| `scenario_id` | 剧本 ID |
| `session_id` | 会话 ID，仅会话记忆需要 |
| `chapter` | 章节 |
| `title` | 文档标题 |
| `chunk_index` | 分块序号 |
| `memory_type` | `rag_chunk`、`session_memory`、`clue_summary` 等 |
| `is_rag_data` | 是否来自 RAG 导入 |
| `data_source` | `rag_pipeline`、`session_summary` 等 |
| `citation` | 前端可展示的引用文本 |

游戏助手默认只允许访问：

- `visibility = public`
- `visibility = player_visible`
- 当前 `session_id` 下已经发现的信息

不允许直接访问：

- `visibility = keeper_only`
- 未发现线索；
- 剧本真相；
- 隐藏 NPC 动机；
- 未触发事件。

---

# 十五、后端 API 调整

保留原有 `/coc/api` 路由。

新增接口：

| 接口 | 方法 | 说明 |
|---|---|---|
| `/coc/api/assistant/chat/stream` | POST | 游戏助手流式问答，`session_id` 可选 |
| `/coc/api/assistant/chat` | POST | 非流式游戏助手问答 |
| `/coc/api/sessions/{session_id}/assistant/messages` | GET | 获取某会话的助手历史消息，可选实现 |
| `/coc/api/sessions/{session_id}/assistant/messages` | DELETE | 清空助手历史消息，可选实现 |

助手请求体建议包含：

| 字段 | 说明 |
|---|---|
| `session_id` | 可选，若提供则允许读取玩家可见会话信息 |
| `message` | 玩家问题 |
| `mode` | `rules`、`session_help`、`auto` |
| `enable_mqe` | 是否启用 MQE，默认 true |
| `mqe_expansions` | MQE 扩展数量，默认 2 或 3 |
| `enable_hyde` | 是否启用 HyDE，默认 auto |
| `top_k` | 返回片段数量 |
| `candidate_pool_multiplier` | 候选池倍数，默认 4 |

助手响应建议包含：

| 字段 | 说明 |
|---|---|
| `answer` | 助手回答 |
| `citations` | 引用来源 |
| `retrieval_debug` | 调试信息，仅调试模式返回 |
| `spoiler_blocked` | 是否触发防剧透 |
| `mode` | 实际使用的回答模式 |

---

# 十六、数据模型调整

## 1. TurnLog 增加字段

`TurnLog` 建议新增 JSON 字段：

| 字段 | 说明 |
|---|---|
| `turn_plan` | 本回合计划 |
| `react_trace` | ReAct 执行轨迹 |
| `tool_observations` | 工具观察结果 |
| `reflection_report` | Reflection 自检报告 |
| `final_guardrail_report` | 最终校验结果 |

这些字段主要用于调试、回放和回归测试。

## 2. 可选新增 AssistantMessage 表

如果希望助手聊天记录随会话保存，可新增 `assistant_messages`。

字段建议：

| 字段 | 说明 |
|---|---|
| `id` | 消息 ID |
| `session_id` | 可为空，无会话时为通用规则问答 |
| `role` | `user` 或 `assistant` |
| `content` | 消息内容 |
| `citations` | 引用来源 JSON |
| `retrieval_trace` | 检索摘要 JSON |
| `created_at` | 创建时间 |

如果暂时不想增加数据库表，也可以先将助手聊天记录保存在前端 `localStorage`，后续再持久化。

---

# 十七、前端状态与流式响应调整

## 1. 主游戏流式接口保持不变

现有：

- `start`
- `chunk`
- `final`
- `error`

继续用于守秘人叙事。

可选新增：

| 事件 | 说明 |
|---|---|
| `status` | 显示“正在规划”“正在检索”“正在裁定”“正在反思”等状态，不暴露思考链 |
| `roll` | 可选展示骰点事件 |
| `debug` | 调试模式下返回计划摘要或工具调用摘要 |

## 2. 助手流式接口独立

助手对话不混入主游戏消息流。

助手流事件建议：

| 事件 | 说明 |
|---|---|
| `start` | 助手开始回答 |
| `retrieval` | 可选，返回检索状态 |
| `chunk` | 回答文本片段 |
| `citations` | 引用来源 |
| `final` | 完整回答 |
| `error` | 错误 |

---

# 十八、防剧透策略强化

## 1. KeeperAgent 防剧透

仍保留现有多层防护：

- Prompt 约束；
- 检索过滤；
- `state_delta` 校验；
- 文本清洗；
- 选项清洗；
- 审计记录。

新增 Reflection 检查：

- 叙事是否暗示隐藏真相；
- 下一步选项是否暴露未发现线索；
- 状态变化是否提前触发剧情；
- 是否把守秘人视角误写成玩家视角。

## 2. 游戏助手防剧透

助手额外规则：

1. 不回答“真相是什么”“幕后黑手是谁”“怪物是什么”等问题。
2. 如果玩家问当前剧本秘密，回答为非剧透提示。
3. 如果玩家问“我现在该去哪”，只能基于已发现线索提示。
4. 如果检索结果包含 `keeper_only`，直接丢弃。
5. 回答中不得引用未发现线索名称。

示例策略：

玩家问：

“灯塔里真正发生了什么？”

助手不应回答真相，而应回答：

“这涉及剧本秘密。你可以从已发现线索出发，回顾目前的地点、物品和异常现象。如果需要，我可以帮你整理你已经发现的信息。”

---

# 十九、测试与验收重点

## 1. Agent 测试

| 测试项 | 目标 |
|---|---|
| 计划生成测试 | 确保 `turn_plan` 结构稳定 |
| 工具白名单测试 | ReAct 不能调用计划外工具 |
| ReAct 上限测试 | 防止无限工具循环 |
| Reflection 修复测试 | 能发现明显规则错误和剧透 |
| 状态提交测试 | LLM 不能绕过 `validate_state_delta` |
| 回合回归测试 | 相同场景下裁定稳定且可追踪 |

## 2. Tools / Skills 测试

| 测试项 | 目标 |
|---|---|
| Tool 输入输出测试 | 保证结构化返回 |
| 骰点记录测试 | 每次检定可回放 |
| Skill 调用链测试 | 调查、移动、交谈、使用物品流程稳定 |
| 无副作用测试 | Tool / Skill 不直接写数据库 |

## 3. 游戏助手测试

| 测试项 | 目标 |
|---|---|
| MQE 测试 | 同一规则问题的多种问法都能召回 |
| HyDE 测试 | 专业规则问题命中正确规则段落 |
| 去重合并测试 | 多查询命中同一片段时不重复 |
| 引用测试 | 回答能展示来源 |
| 防剧透测试 | 不泄露未发现剧情 |
| 前端收起测试 | 助手面板状态可保存 |

---

# 二十、修改后版本总结

修改后的 `coc-lite` 保持原有技术栈：

- React / Vite / TypeScript 前端；
- FastAPI 后端；
- LangGraph 编排；
- PostgreSQL 持久化；
- Chroma 向量检索；
- OpenAI 兼容 LLM；
- 千问 `text-embedding-v4` 作为默认 Embedding。

核心变化是：

1. `KeeperAgent` 从线性节点升级为 **Plan-and-Solve + ReAct + Reflection** 的混合 Agent。
2. 只有需要 LLM 自主选择的复用能力才抽象为 **Tools / Skills**。
3. 状态加载、状态提交、防剧透、校验、数据库写入等关键流程仍由代码写死。
4. 前端新增可收起的 **游戏助手对话栏**。
5. 游戏助手使用 **MQE + HyDE + 统一扩展检索框架** 提高规则问答质量。
6. 游戏助手与守秘人主流程隔离，不推进剧情、不修改状态。
7. 防剧透从主 Agent 扩展到助手 RAG 检索与回答生成全过程。

最终目标是：

> 让 AI 守秘人更会规划、更会调用能力、更会自我修正；
> 同时让玩家能随时通过游戏助手查询规则，而不会被剧透或破坏游戏状态。